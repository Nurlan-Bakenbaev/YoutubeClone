import { Stack } from "@mui/material"

const Navbar = () => {
  return (
    <Stack direction='row' alignItems={'center'} p={'2'} sx={{ position:'sticky', background:'black',top:'o'}}>

    </Stack>
  )
}

export default Navbar